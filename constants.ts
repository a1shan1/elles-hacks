import { VibeType, CategoryType } from './types';

export const VIBES = Object.values(VibeType);
export const CATEGORIES = Object.values(CategoryType);

export const SUGGESTED_SEARCHES = [
  "Late night bubble tea with wifi",
  "Quiet places near U of T to study",
  "Tech networking events this week",
  "Cheap eats near TMU for lunch",
  "Cozy cafes in Kensington Market"
];

// Removed Y2K_ICONS
